<div class="mt-2">
    <div class="text-center">
        <%= image_tag @post.thumbnail.url, class:"img-fluid" if @post.thumbnail? %>
    </div>
    <div>
        <h1 class="text-center mt-2">
            <%= @post.title %>
        </h1>
        <p>
            <%= @post.body%>
        </p>
    </div>
    <div class="row"></div>
</div>
